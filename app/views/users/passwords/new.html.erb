<div class="flex flex-col items-center justify-start pt-10 min-h-screen bg-base-100">
  <div class="card w-full max-w-sm bg-orange-200 shadow-xl">
    <div class="card-body">
      <h1 class="card-title justify-center">パスワード忘れてしまった場合</h1>
      <h2 class="items-center size-xl">
        🚧現在改修中🚧 <br />
        メールは飛びません
      </h2>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "users/shared/error_messages", resource: resource %>

        <div class="form-control mb-4">
          <%= f.label :email, "メールアドレス", class: "label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input input-bordered" %>
        </div>

        <div class="form-control mt-6">
          <%= f.submit "送信", class: "btn btn-warning" %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="pt-10">
    <%= render "users/shared/links" %>
  </div>
</div>
